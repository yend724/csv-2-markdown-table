{
	"name": "csv-2-markdown-table",
	"private": true,
	"version": "0.0.0",
	"scripts": {
		"dev": "npm run dev --ws --if-present",
		"build": "npm run build --ws --if-present",
		"preview": "npm run preview --ws --if-present",
		"lint": "npm run lint --ws --if-present"
	},
	"workspaces": [
		"packages/app"
	],
	"dependencies": {
		"@tailwindcss/postcss": "^4.1.8"
	}
}
